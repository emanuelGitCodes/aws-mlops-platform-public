# The backend image owns its dependencies. `uv.lock` governs the repository
# environment and does not reach this container. Keep boto3 and pydantic equal
# to the locked versions, so the container validates records the same way the
# platform does.
boto3==1.43.59
pydantic==2.13.4
fastapi==0.139.0
uvicorn==0.38.0
